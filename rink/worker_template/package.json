{
  "name": "rink-serve-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy --config wrangler.jsonc",
    "dry-run": "npx wrangler deploy --dry-run --outdir dist --config wrangler.jsonc",
    "types": "npx wrangler types --config wrangler.jsonc"
  }
}
